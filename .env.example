# Telegram Bot Token (get from @BotFather)
BOT_TOKEN=your_bot_token_here

# Webhook Configuration (for production deployment on Render)
# Set to "true" for webhook mode, "false" for polling mode
USE_WEBHOOK=true

# Your Render app URL (e.g., https://your-app-name.onrender.com)
WEBHOOK_URL=https://your-app-name.onrender.com

# Port (Render automatically sets this, default is 10000)
PORT=10000

# Data Directory for persistent storage (IMPORTANT!)
# For Render: Set to /data (after creating a persistent disk)
# For local: Leave as . (current directory)
DATA_DIR=/data

